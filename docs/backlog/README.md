# Backlog Structure

- `features/` for new capabilities.
- `bugfix/` for regressions and defects.
- `refactor/` for internal structure changes without intended behavior change.
- `tests/` for testing-only tasks.
- `roadmaps/` for larger multi-task plans.

Use `/task` to create new items from the templates in `docs/templates/`.